package softeer2nd.chess.pieces;

import static softeer2nd.chess.pieces.Chess.*;
import static softeer2nd.chess.pieces.Chess.Color.*;

public class PieceFactory {
    public static Chess createChess(Color color, Type type){
        if(color.equals(WHITE)){

        }else if(color.equals(BLACK)){

        }else{

        }
    }

    public static Chess createWhite(Type type) {
        switch (type) {
            case PAWN:
                return
        }
    }
    public static Chess createBlack(Type type) {

    }
}
